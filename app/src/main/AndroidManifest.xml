<?xml version="1.0" encoding="utf-8"?>
<!-- App-Manifest: deklariert die App, die nötige Internet-Berechtigung
     (nur für den KI-Analyse-Aufruf) und die einzige Start-Activity -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Internet wird ausschließlich für den NVIDIA-Vision-API-Aufruf benötigt;
         alle Trade-Daten werden lokal auf dem Gerät gespeichert. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:label="${appLabel}"
        android:supportsRtl="true"
        android:theme="@style/Theme.TradingAnalyzer">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
